import styled from '@emotion/styled';

const Button = styled.button`
  color: #eee;
  background: #0F4C81;
  padding: 0.5rem;
  outline: none;
  border: none;
  border-radius: 4px;
  cursor: pointer;
`;

export default Button;
